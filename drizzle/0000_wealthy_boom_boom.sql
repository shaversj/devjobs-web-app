CREATE TABLE `jobs` (
	`id` integer PRIMARY KEY NOT NULL,
	`company` text,
	`logo` text,
	`logoBackground` text,
	`position` text,
	`postedAt` text,
	`contract` text,
	`location` text,
	`website` text,
	`apply` text,
	`description` text,
	`requirements` text,
	`role` text
);
